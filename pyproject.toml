[project]
name = "model-interface"
version = "0.1.3.dev0"
description = "Model interface for VLM HyperBench"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
authors = [
   {name = "Anton Shiryaev", email = "med.phisiker@gmail.com"}
]

[build-system]
requires = ["uv_build>=0.8.2,<0.9.0"]
build-backend = "uv_build"

[project.urls]
Repository = "https://github.com/VLMHyperBenchTeam/model_interface"
Homepage = "https://github.com/VLMHyperBenchTeam"
